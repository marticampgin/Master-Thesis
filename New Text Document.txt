  # If greetings are not present, check for the [on ... wrote]-pattern
            elif not greetings_result:
                # Can be added as a parameter n in the method afterwards
                # Since patterns can be split across multiple lines
                n = 3
                first_n_lines = body_lines[:n]

                # Here, only SEARCH is appropriate, as patterns could occur in the middle
                for i, line in enumerate(first_n_lines):
                    date_result = re.search(dates_regex, line)
                    email_wrote_result = re.search(email_wrote_regex, line)
                    if date_result or email_wrote_result:
                        body_lines[i] = ''
                     